const names = ["wande", "tunde", "allde"];

//  export {names}

module.exports = names;

/*
because of the "type": "module" which was included in the package.json, to use CommonJs module exports
js files have to be converted to have .cjs extensions . Then and only then do the CommonJs exports work.
*/
