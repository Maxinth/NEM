"use strict";

let messages = [
  "A change of environment can be a good thing!",
  "You will make it",
  "Just run with the code",
];

/*

TO SAVE FILE-SESSIONS using .save at the repl

load a file in , say main.js

- SUPPLY node at terminal
- .load main.js

- *******
repl now has access to messages as it has just been loaded in.
messages.forEach((msg) => console.log(` * ${msg}`));

after getting the output

all that has been done can be saved to a file, say a file inside the moreTests folder called displayMsg.js
this is done by supplying .save moreTests/displayMsg.js at the repl





*/
